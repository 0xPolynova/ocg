# Server-only. Never prefix these with NEXT_PUBLIC_ — they must not ship to the browser.
OPENROUTER_API_KEY=
OPENROUTER_MODEL=google/gemini-2.5-flash
SOLANA_RPC=https://api.mainnet-beta.solana.com
API_PORT=4000
CORS_ORIGIN=http://localhost:3000,https://launchocg.com,https://www.launchocg.com

# Frontend. Points at the API server (local or Render).
NEXT_PUBLIC_API_URL=http://localhost:4000
